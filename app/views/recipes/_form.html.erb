<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  <ul>
    <%= f.fields_for :ingredients do |i| %>
      <li>Name: <%= i.text_field :name %></li>
      <li>Quantity: <%= i.text_field :quantity %></li>
    <% end %>
  </ul>

<%= f.submit %>
<% end %>
